
export default {

	/**
	 * SIZE
	 * decides the width
	 * @type string (small|medium|large)
	 */
	size: 'medium', // decides the width          

	/**
	 * DISPLAY
	 * decides the display type
	 * @type string (scroll|center)
	 */
	display: 'scroll',

	/**
	 * backdrop
	 * whether to allow backdrop elements to be clickable
	 * @type boolean
	 */
	backdrop: true

};